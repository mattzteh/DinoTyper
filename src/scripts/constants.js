export const words = 'public business need long number word about after much need open change also in one good real one not school set they state against person system interest general point'.split(' ')
export const wordsCount = words.length;

export const wordsDiv = document.getElementById('words');
export const game = document.getElementById('game');
export const resetTest = document.getElementById('reset');
export const cursor = document.getElementById('cursor');
export const clock = document.getElementById('info');

export const dino = document.getElementById('dino-img');
export const human = document.getElementById('human');
export const message = document.getElementById('message');
export const desktop = document.getElementById('desktop-msg');

